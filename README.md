# AI 日报

前端预览。Next.js App Router + TypeScript，数据全部本地 mock，不接抓取和模型。

```bash
npm install
npm run dev
```

打开 http://localhost:3000

- `/` 今日早报
- `/d/2026-09-13` 历史早报
- `/story/claude-memory` 事件簇
- `/archive` 月历
- `/topics` 专题
- `/sources` 信源健康
- `/search` 搜索，`/` 或 `Ctrl+K` 进入